<?xml version="1.0" encoding="utf-8"?>
<!-- 量产(release)构建：禁止明文 HTTP，仅允许 HTTPS。
     LLM 地址为运行期配置，不在此处按域名放行。 -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
